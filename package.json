{
  "name": "contacts",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run tailwind:watch\" \"./venv/bin/python -m flask --app wsgi run --port 8000 --debug\" \"npm run watch:reload\"",
    "tailwind:watch": "npx @tailwindcss/cli -i ./templates/css/input.css -o ./templates/css/output.css --watch",
    "tailwind:build": "npx @tailwindcss/cli -i ./templates/css/input.css -o ./templates/css/output.css",
    "watch:reload": "browser-sync start --proxy http://127.0.0.1:8000 --files templates/**/*.html templates/css/output.css app/**/*.py --no-ui --no-open --reload-delay 0"
  },
  "devDependencies": {
    "browser-sync": "^2.29.3",
    "concurrently": "^8.2.2",
    "tailwindcss": "^4.1"
  }
}